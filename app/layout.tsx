import type { Metadata } from 'next';
import './globals.css';
import Analytics from './analytics';

export const metadata: Metadata = {
  title: 'Seraphova — Your chart. Your sky. Every morning.',
  description: 'Daily personalized horoscope based on your full natal chart. One-time payment, 365 readings delivered to your inbox.',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link
          href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400;1,500&family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;1,9..40,300&display=swap"
          rel="stylesheet"
        />
      </head>
      <body>
        {children}
        <Analytics />
      </body>
    </html>
  );
}
